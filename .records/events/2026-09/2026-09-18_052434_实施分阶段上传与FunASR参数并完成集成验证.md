---
record_id: rec_20260918_052434_60c942
occurred_at: 2026-09-18T05:24:34+08:00
kind: artifact
domain: engineering
certainty: verified
related_artifacts:
  - https://github.com/shishengkai/magicdub-skills
  - Drafts/magicdub-skills/fun-asr-upload-validation/README.md
  - Drafts/magicdub-skills/fun-asr-upload-validation/runs/integrated-routing/summary.json
  - Releases/05_magicdub-skill项目规划.md
supersedes: null
---

# 分阶段上传与 Fun-ASR 配置、参数及音频处理已本地实施

## 结果

用户要求实施此前所有修改决定。本地 magicdub-skills 已落实：分离、Whisper、TTS 和回转写按步骤上传至 fal；Fun-ASR 使用百炼临时存储。上传缓存按步骤、供应商、模型、账号、Base URL、内容哈希隔离，12 小时复用，上传失败不回退其他服务。

Fun-ASR 输入统一为 16 kHz 单声道 PCM16 WAV，保留高品质分离原件及完整时间轴；新参数为 diarization_enabled=true，special_word_filter 作为 JSON 字符串传入，filter_with_signed.word_list 仅含“肏屄”，system_reserved_filter=false。已有项目参数快照不变；显式外部 URL 新提交必须绑定处理后音频哈希，已有任务直接续查。

DASHSCOPE_HTTP_BASE_URL 已由程序读取：进程环境优先，其后 credentials.env、config.json、公共北京地址；setup 同步 env 和 JSON，doctor 显示实际地址。当前用户 env、JSON 和生效值均匹配已提供的北京专属域名。北京专属域名费用估算已修复；导入凭据保留其他配置。配置样例、README、参数说明与 setup skill 已同步。

## 验证

221 项完整回归通过；随后补充空凭据导入边界修复及用例，配置／上传／Fun-ASR 相关 29 项通过。Ruff、diff 空白检查及 49 文件清单哈希通过。本地源码为验证对象，避免使用工作区旧 wheel。

独立 32 秒双人样片经过真实音频副本处理、百炼凭证获取／上传、Fun-ASR 识别、结果下载及正式 pipeline 解析：仅 1 次上传及 1 次识别提交，HTTP 200，最终 succeeded；7 句时间有效，零 fal 调用。复读成功结果零新增网络请求。原文件哈希不变，已合规输入生成副本后字节相同；非标准双声道输入转换另有本地测试。

返回 usage.duration=21，程序估算 ¥0.004620，未对账。任务 ID 为 c9541d64-3470-417b-8926-716fdc6a2d23；脱敏证据与运行脚本见关联 Draft，原始响应及临时地址在其忽略的 private 目录。

## 边界

双人样片仍返回 3 个说话人标签，存在过分割；未验收自然多人、过滤词真实替换效果或完整译制。未切换默认 ASR、改写旧项目、升级日常安装或运行额外分离／TTS。本地代码仍基于 v0.5.1，未提交、推送或正式发布；release.json 仅更新本地文件摘要。
