# SnapAny 解析与计费验证

验证时间：2026-09-15（Asia/Shanghai）。状态：**解析与计费通过；媒体下载和解码未验证，不能作为自动下载方案验收。**

## 测试范围与操作

用户已注册并领取 50 个免费积分，明确允许创建名为「MagicDub 验证」的 API Key。通过 SnapAny 官方 API 参考页的请求表单，向 `POST /openapi/v1/extract/post` 提交一次此前指定的样片：https://www.youtube.com/watch?v=ymuWHoWMlNQ 。没有再次解析、充值、启动转录或 MagicDub 模型。

官方调试页返回真实 HTTP 200，耗时约 5.6 秒；视频 ID 匹配，标题为「✨My Journey With Chinese✨」，标称时长 367 秒。保存的是 43,076 字节的接口 JSON 响应，**不是下载完成的视频或音频**。原始响应 SHA-256：`f6396bbd351a7c91fda8a9c1870eeac24323cac81e84e70ebcc99a2a0f240393`。本文不复制临时签名地址或 API Key。

## 实际返回格式

下列规格和大小均来自接口响应，未经过下载文件的 ffprobe 或完整解码验证。

| 项目 | 最高／相关可用选项 | 接口声明字节数 |
| --- | --- | ---: |
| 独立视频 | 1440p、30 fps、VP9、WebM，itag 271 | 170,563,124 |
| 英语原声候选 | Opus、WebA，itag 251，en-US | 6,653,442 |
| 英语原声备选 | AAC、M4A，itag 140，en-US | 5,944,505 |
| H.264 视频备选 | 1080p、30 fps、MP4，itag 137 | 68,357,887 |

- 共返回 7 档视频：1440、1080、720、480、360、240、144；顶层默认 `resource_url` 为 360p，不代表最高品质。最高视频及 Opus 音频在视频 `variants` 中；音频 `medias` 的默认项为 AAC，不能只取默认项来宣称最高规格。
- 独立音频 variants 含英语原声及 11 种自动译配语言；本次源语言应选 en-US，不能误选自动译配音轨。
- 原声音轨地址的公开规格标记含 `acont=original:drc=1:lang=en-US`，不能宣称未经动态处理的上传母带。Opus 与 AAC 的真实可解码性及听感尚未验证。
- 本次响应还含英语字幕的多格式地址；没有另行请求字幕接口或下载字幕。
- 本次没有返回媒体专用 `headers` 对象；不能把文档中的示例请求头当作本次实际配置。

## 下载边界

所有音视频目标均为 Googlevideo 的 `/videoplayback`，本次 host 为 `rr1---sn-jn2pgx4pcxg-w5oz.googlevideo.com`，客户端标记为 `WEB_EMBEDDED_PLAYER`。没有返回 SnapAny 独立托管的完整媒体文件。

此前当前宿主浏览器工具已明确拒绝 Googlevideo 下载动作，并禁止用其他工具或间接方式绕过。此次只验证新解析接口与返回数据，没有发起这些媒体地址的下载，没有切换 HTTP 工具、代理或浏览器。因此：

- 未观察到本次媒体的 HTTP 403，也没有媒体下载成功证据。
- 不能认定 SnapAny 的下载链接无效，也不能宣称该路线已完成全流程。
- 后续只有在允许的下载方式中取得完整文件，再检查规格、时长、解码和正确原声音轨后，才满足接入 skill 的前提。

## 真实费用

控制台积分明细于 `2026/09/15 00:43:06` 记录 `extract.post`、`-1` 积分、余额 `49`；与测试前 50 积分一致。

- 实际消耗：1 个赠送积分。
- 实际现金支出：¥0.00。
- 按最低付费套餐 ¥69 / 9,000 积分折算的调用价值：约 ¥0.007667，仅为折算值，不是本次现金扣款。
- 控制台套餐数量确认 ¥1,399 对应 230,000 积分、¥3,499 对应 650,000 积分；与首页一致，计费文档的这两档存在漂移。

## 保全与后续

magicdub-skills 源代码仍为正式 v0.5.0 / `ffb0e31890ece9f2bf451652446b5e7b0c44d456`，工作区干净。本次没有新增下载实现、修改 skill、安装 MCP、保存用户级凭据文件、提交或发布。测试 Key 保留在用户已授权创建的 SnapAny 账号中，调试输入框及临时剪贴板已清空；没有复制凭据到 brain。

参考：[提取接口](https://platform.snapany.com/zh/docs/extract-post)、[MCP 的文件交付边界](https://platform.snapany.com/zh/docs/mcp)、[积分明细](https://platform.snapany.com/zh/console/usage)。
